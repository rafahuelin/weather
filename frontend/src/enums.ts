export enum TimeAggregation {
  NONE = 'None',
  HOURLY = 'Hourly',
  DAILY = 'Daily',
  MONTHLY = 'Monthly',
}

export enum DataType {
  TEMPERATURE = 'temperature',
  PRESSURE = 'pressure',
  SPEED = 'speed',
}
